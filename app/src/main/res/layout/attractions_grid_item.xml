<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:gravity="center"
    android:padding="1dp">

    <ImageView
        android:id="@+id/attractions_grid_item_image"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:padding="1dp"
        android:scaleType="fitXY"
        />

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="right|bottom"
        android:orientation="vertical"
        android:layout_alignLeft="@id/attractions_grid_item_image"
        android:layout_alignTop="@id/attractions_grid_item_image"
        android:layout_alignRight="@id/attractions_grid_item_image"
        android:layout_alignBottom="@id/attractions_grid_item_image"
        >

        <TextView
            android:id="@+id/attractions_grid_item_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="right|bottom"
            android:gravity="right|bottom"
            android:layout_margin="10dp"
            android:textSize="@dimen/font_size_normal"
            android:textColor="@android:color/white"
            android:textStyle="bold"
            android:background="@drawable/city_pager_bg"
            />
    </LinearLayout>

</RelativeLayout>
