<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    android:layout_margin="16dp"
    tools:context="xyz.narengi.android.ui.activity.HouseFeaturesActivity"
    android:background="@android:color/white"
    >

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/house_features_dataLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginBottom="80dp"
        android:background="@android:color/white"
        >

        <xyz.narengi.android.ui.widget.CustomTextView
            android:id="@+id/house_features_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="16dp"
            android:layout_marginTop="16dp"
            android:background="@android:color/white"
            android:layout_gravity="center"
            android:gravity="center"
            android:text="@string/house_features_dialog_title"
            android:textColor="@color/green_dark"
            style="@style/TextAppearance.AppCompat.Large"
            android:textStyle="bold" />

        <View
            android:id="@+id/house_features_divider"
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginBottom="8dp"
            android:layout_marginLeft="16dp"
            android:layout_marginRight="16dp"
            android:layout_marginTop="0dp"
            android:background="@android:color/darker_gray"
            />
        <!--android:background="@android:color/tertiary_text_dark"-->

        <ListView
            android:id="@+id/house_features_list"
            android:layout_width="fill_parent"
            android:layout_height="match_parent"
            android:divider="@null"
            android:isScrollContainer="true"
            android:scrollbars="vertical"
            android:smoothScrollbar="true"
            android:background="@android:color/white"
            />

    </LinearLayout>


    <xyz.narengi.android.ui.widget.CustomButton
        android:id="@+id/house_features_closeButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_margin="16dp"
        android:background="@drawable/dialog_button_bg"
        android:shadowColor="@android:color/transparent"
        android:shadowRadius="0"
        android:elevation="0dp"
        android:stateListAnimator="@null"
        android:gravity="center"
        android:text="@string/close_button"
        android:textColor="@android:color/tertiary_text_dark"
        style="@style/TextAppearance.AppCompat.Medium"/>

</RelativeLayout>
