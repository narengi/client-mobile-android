<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/DrawerLayoutMain"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context="xyz.narengi.android.armin.view.activities.MainActivity">


    <FrameLayout
        android:id="@+id/FrameLayoutFragmentContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <android.support.design.widget.NavigationView
        android:id="@+id/MainNavigationView"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="right"
        app:headerLayout="@layout/navigation_view_header"
        app:itemTextColor="@color/orange_light"
        app:menu="@menu/menu_navigation_view">

        <TextView
            android:id="@+id/TextViewHosting"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:background="@color/gray_light"
            android:drawableLeft="@drawable/host_ic"
            android:gravity="right|center"
            android:padding="8dp"
            android:text="@string/drawer_menu_hosting"
            android:textColor="@color/orange_light" />

    </android.support.design.widget.NavigationView>
</android.support.v4.widget.DrawerLayout>
